# shared

Shared utilities and types for the RetroPick monorepo (`sc-cre-workflow-chainlink`).

## Usage

```bash
bun install
bun run index.ts
```

This package is intended for code shared across `apps/workflow`, `apps/relayer`, and `apps/front-end-v2` (e.g. types, config schemas, constants).
